<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.xuli.toe.dao.IItemTypeDao">
    <select id="getItemTypes" resultType="team.xuli.toe.domain.ItemType">
        SELECT 
        item_type_dict.itemTypeId,
        item_type_dict.name,
        item_type_dict.description,
        item_type_dict.status
        FROM item_type_dict
        WHERE status = 1;
    </select>
</mapper>

